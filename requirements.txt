# CME 数据下载器依赖包
# Python 3.8+ 兼容

# HTTP 请求库
requests>=2.31.0

# HTML 解析库
beautifulsoup4>=4.12.0

# HTML 解析器（BeautifulSoup 的后端）
lxml>=4.9.0

# 可选：数据库支持（如需要）
# SQLAlchemy>=2.0.0  # ORM 框架
# PyMySQL>=1.1.0     # MySQL 驱动
# psycopg2-binary>=2.9.0  # PostgreSQL 驱动

# ==================== ETL 数据处理依赖 ====================
# 数据处理库
pandas>=2.0.0

# Excel 文件支持
openpyxl>=3.1.0       # 读写 .xlsx 文件
xlrd>=2.0.0           # 读取旧版 .xls 文件

# PDF 解析库（处理 CME 交割通知 PDF）
pdfplumber>=0.10.0    # PDF 表格提取（推荐）
